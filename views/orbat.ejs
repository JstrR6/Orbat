<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/htmx.org@1.9.4"></script>
    <style>
        .hot-bar {
            transition: background-color 0.3s ease;
        }
        .hot-bar:hover {
            background-color: rgba(107, 114, 128, 0.3);
        }
    </style>
</head>
<body class="bg-gray-100">
    <!-- Hot Bar -->
    <div class="fixed top-0 left-0 right-0 z-50">
        <div class="hot-bar flex justify-center items-center h-16 px-4">
            <div class="flex space-x-4">
                <a href="/rank-structure" class="text-black hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium">
                    Rank Structure
                </a>
                <a href="/orbat" class="text-black hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium">
                    ORBAT
                </a>
                <a href="/forms" class="text-black hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium">
                    Forms
                </a>
                <a href="/orders" class="text-black hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium">
                    Orders By The General
                </a>
            </div>
        </div>
    </div>

    <div class="container mx-auto px-4 pt-20">
        <h1 class="text-3xl font-bold mb-6">ORBAT Viewer</h1>
        
        <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
            <% function renderUnit(unit) { %>
                <div class="mb-4">
                    <h2 class="text-xl font-semibold"><%= unit.name %> (<%= unit.type %>)</h2>
                    <div class="ml-4">
                        <p>Commander: <%= leadership[unit.id]?.commander || 'Not assigned' %></p>
                        <p>Deputy Commander: <%= leadership[unit.id]?.deputyCommander || 'Not assigned' %></p>
                        <p>Senior Enlisted Leader: <%= leadership[unit.id]?.seniorEnlistedLeader || 'Not assigned' %></p>
                        
                        <!-- Leadership Update Form -->
                        <form hx-post="/api/update-leadership" hx-swap="none" class="mt-2">
                            <input type="hidden" name="unitId" value="<%= unit.id %>">
                            <select name="position" class="mr-2 p-1 border rounded">
                                <option value="commander">Commander</option>
                                <option value="deputyCommander">Deputy Commander</option>
                                <option value="seniorEnlistedLeader">Senior Enlisted Leader</option>
                            </select>
                            <input type="text" name="name" placeholder="New leader name" class="mr-2 p-1 border rounded">
                            <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-1 px-2 rounded">
                                Update
                            </button>
                        </form>
                    </div>
                    
                    <% if (unit.subordinates && unit.subordinates.length > 0) { %>
                        <div class="ml-8">
                            <% unit.subordinates.forEach(subUnit => { %>
                                <% renderUnit(subUnit) %>
                            <% }); %>
                        </div>
                    <% } %>
                </div>
            <% } %>

            <% renderUnit(structure) %>
        </div>
    </div>

    <script>
        document.body.addEventListener('htmx:afterRequest', function(event) {
            if (event.detail.successful) {
                alert('Leadership updated successfully');
                location.reload(); // Reload the page to reflect changes
            } else {
                alert('Failed to update leadership');
            }
        });
    </script>
</body>
</html>