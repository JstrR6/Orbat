<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>United States Military Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        purple: {
                            600: '#9333ea',
                            700: '#7e22ce',
                        }
                    }
                }
            }
        }
    </script>
</head>
<body class="min-h-screen flex flex-col items-center justify-center bg-gray-900 relative overflow-hidden">
    <div id="glow" class="absolute inset-0 opacity-50"></div>
    
    <h1 class="text-4xl font-bold text-white mb-8 text-center px-4">United States Military Dashboard</h1>
    
    <div class="w-full max-w-md bg-white/10 backdrop-blur-sm text-white mx-4 rounded-lg shadow-xl">
        <div class="p-6">
            <h2 class="text-2xl font-bold text-center mb-6">Login</h2>
            <div class="flex justify-center">
                <% if (!user) { %>
                    <a href="/auth/discord" id="discord-login" class="w-full max-w-xs bg-purple-600 hover:bg-purple-700 text-white py-2 px-4 rounded-lg flex items-center justify-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1" />
                        </svg>
                        Login with Discord
                    </a>
                <% } else { %>
                    <div class="text-center">
                        <p class="mb-4">Welcome, <%= user.username %>!</p>
                        <a href="/dashboard" class="w-full max-w-xs bg-purple-600 hover:bg-purple-700 text-white py-2 px-4 rounded-lg inline-block mb-2">Go to Dashboard</a>
                        <a href="/logout" class="w-full max-w-xs bg-red-600 hover:bg-red-700 text-white py-2 px-4 rounded-lg inline-block">Logout</a>
                    </div>
                <% } %>
            </div>
        </div>
        <div class="px-6 pb-6 flex justify-center text-sm text-gray-400">
            Secure access for authorized personnel only
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const glow = document.getElementById('glow');

            document.addEventListener('mousemove', (e) => {
                glow.style.background = `radial-gradient(circle at ${e.clientX}px ${e.clientY}px, rgba(59,130,246,0.3) 0%, rgba(59,130,246,0) 60%)`;
            });
        });
    </script>
</body>
</html>
